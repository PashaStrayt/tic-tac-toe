export enum FigureType {
  mark = 'mark',
  zero = 'zero'
}

export enum FigureSize {
  primary = 92,
  secondary = 70,
  tertiary = 50,
  quaternary = 35,
  quinary = 23,
  senary = 16
}

export enum FigureText {
  mark = '☓',
  zero = '0'
}

export enum FigureColor {
  mark = 'orange',
  zero = 'blue'
}